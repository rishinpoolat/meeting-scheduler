# Plan: [Feature Name]

**Status:** Pending Approval | Approved
**Spec:** see ../spec.md (if one exists)
**Branch:** (filled in once created, per git-workflow skill)

> This file is created only AFTER the developer approves the plan
> proposed in plan mode. Nothing here should be implemented before that
> approval is given.

## To Do

- [ ] Sub-task 1
- [ ] Sub-task 2
- [ ] Unit tests: (list specific cases/edge cases this covers, not just
      "add unit tests")
- [ ] Integration test: (only if this feature has a meaningful end-to-end
      flow worth testing as a whole — e.g. full auth handshake, full
      checkout. Skip if the feature is simple enough that unit tests
      alone cover it; say so explicitly rather than leaving this blank)

## In Progress

(moved here once work starts)

## Completed

(moved here once test-runner passes, code-reviewer has reported on both
implementation and test quality, AND the developer has given explicit
pre-commit approval — see testing skill)
